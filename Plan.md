README中增加cudd和sdd的准备部分，可以参考这一部分的dockerfile：# CUDD (new org) with autoreconf
WORKDIR /opt
RUN git clone --depth 1 https://github.com/cuddorg/cudd.git /opt/cudd \
 && cd /opt/cudd \
 && autoreconf -fiv \
 && ./configure --enable-shared --prefix=/usr/local CFLAGS="-O3 -fPIC" CXXFLAGS="-O3 -fPIC" \
 && make -j"$(nproc)" && make install && ldconfig

# SDD++ (bundles SDD 2.0)
RUN git clone --depth 1 https://github.com/black-sat/sddpp.git /opt/sddpp \
 && cmake -S /opt/sddpp -B /opt/sddpp/build \
      -DCMAKE_BUILD_TYPE=Release \
      -DCMAKE_POSITION_INDEPENDENT_CODE=ON \
 && cmake --build /opt/sddpp/build -j"$(nproc)" \
 && cmake --install /opt/sddpp/build && ldconfig


增加一个folder放置一个running example，里面是一个简单的case，示例souffle的使用方式。提供一个脚本，里面包括用souffle编译对应case并允许。提供一个简单的readme解释这个case，以及相关的.gitignore；并给出一个step-by-step的指导让用户自行尝试改动程序或者输入。

TODO list

1. The way to compile with Souffle is not simply `souffle -o compute`; your context summary seems off. Please check how `side_channel_inc.py compile` actually constructs the command line to invoke Souffle. You will likely need to operate without the .py script later, so add the relevant details to `README.codex.md`.
2. Earlier results show the third phase of inc-regional takes much longer than full and inc-naive, mainly due to forward compilation time (1.682315). More specifically, analyze and rebuild take a long time. Can you further profile the rebuild phase (mainly) and the subcomputations within analyze? I want to know where the time goes. In theory, rebuild should be the main DD rebuild time and should be faster than inc-naive and full. When profiling, also collect CUDD reordering time; it may account for some time and should be separated.
4. Dred delete is significantly slower; some set operations may need optimization. First, summarize the current Dred implementation, mainly by inspecting the C++ generated by Synthesiser, to see whether it can be optimized.

Then recompile and run, check consistency, and report timings.

Confirm whether the results are still consistent.

You have now added two modes for full: default is full-hard, and there is also full-soft. The difference is calling resetHard vs reset. setmode full defaults to full-hard. You also adjusted the related options.

Now run full(hard), inc-naive, and inc-regional on P12-P15, and extract detailed per-stage profiling comparisons for each of the three turns in each case. `README.profile.md` has guidance.

[inc-analyze] timing(ms): leastParents=14.331027 scopes=8.799561 reach=2.357730 initRegion=2.527037 classify=1.346152 expand=0.041705 upstreamClose=2.195190 reclassify=0.000000 reexpand=0.000000 deltaReach=8.253815 intersect=0.273988 mergeable=0.000189 total=40.126394


<!-- Now, is the complexity of rewriting SISO related to the subgraph size or the full graph size (i.e., do we effectively operate on the full graph to build a new full graph, so even small changes cost full-graph complexity)? Also, for P13, can we still guarantee consistency between facts.prob, no rewrite, and rewrite?

## SISO detection adjustments

- [x] Define the simplest SISO (a special SISO): it contains only si and so and a single edge from si to so (this edge's only input is SI). For this SISO, additionally require that si has no other outgoing edges and so has no incoming edges; otherwise it is not considered SISO (i.e., for two-node SISO, si/so out-degree and in-degree are also restricted). You can mark this SISO specially to make rewrite recognition easier. In logs, when reporting SISO counts, also report the count of this class.
- [ ] Detection should control target SISO size/type
- [ ] Detection should mark other special (trivial/etc.) SISOs for rewriter fast paths
- [ ] Single-conj SISO + input facts combinations (is the current handling of input facts OK?)

The analyzer infrastructure currently feels aimed at finding complex SISOs, but in the side-channel example, very simple SISOs are already useful: they usually have only one or two edges (we can focus on one-edge first). Can you archive the old analyzer logic without changing the interface exposed to the rewriter, and implement a fast path that only detects SISOs with at most two edges? For one-edge SISOs, include pure-two-nodes and general SingleHyperedge. For two-edge SISOs, typical forms are linear (a/SI -> b -> c/SO) or parallel (a -> b / a ->' b), and there can also be (a1 -> b / a2 -> b where a1/a2 are input facts, i.e., all facts as input). Note only pure-two-nodes has extra SI/SO constraints; all-facts-as-SI also has special constraints requiring each fact to have no other incoming edges. I need you to add region kinds for these fast paths, and implement a fast detection path in the analyzer to avoid invoking the heavy detection infrastructure. What do you think?

Consolidate the definitions for all fast-path SISO subgraphs.

For linear two-edge SISO rewrite, replace the two edges and the middle node with a single edge. Emphasize that the middle node in linear two-edge must satisfy SISO constraints (non-query/evidence, rejected during detection). No constraints on si/so. The new edge's probability is the product of the previous two edges' probabilities.

## Graph rewriter optimizations

- [ ] fast path. For fast-path SISO,
- [x] DD manager initialization should only happen when needed; treat it as a thunk.

Thought: for the current side channel, is decomposition weak because conjunctions are dense and there are no disjunctions?

At the end of each round, simplify each hyperedge once so it absorbs lone leaf nodes?

Optimizations for all-conj SISO.

Would on-the-fly analyze + rewrite help?

More detailed description of the current performance bottleneck.

## Notes for evaluation:

- [ ] In BDD build timing, add output for variable reordering time (available from CUDD)
- [ ] Unify how debug mode is enabled

Run P5/P9/P12; compare rewrite vs no-rewrite results, compare stage timings, and compare final forward-comp BDD node counts.

After each iteration that tries to rewrite all SISOs, try shrinking each edge: traverse each edge, find all its fact inputs that are not query/evidence, and fold them into the edge. Replace the edge with a new edge that removes those fact inputs and keeps other inputs unchanged; the new edge's probability becomes the original probability times the probabilities of those fact inputs.

I suddenly remembered that the earlier rewrite logic did not handle negation -- edge semantics are always tied to bodynegations. When computing probabilities, you need to check whether the corresponding input is negated and decide whether to negate; when generating edges, you must also consistently generate bodynegation info. Please fully check all existing rewrite logic to ensure negation is handled correctly.

## engineering

When graphs are small, if a BDD manager is needed, can we reduce initialization memory?

Optimizations for all-AND subgraphs.

Avoid the final forward comp by directly computing the query node's formula and probability.

detect and rewrite on the fly.


## negation handling

For linear two-edge, I think if the edge connected to si is negated, it can still be handled: the new edge's bodynegation should mark si as negated, because the fusion is of the middle two edge variables rather than si, so si is untouched. But if the second edge is negated, then it does not work. -->
