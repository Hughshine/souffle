.decl b(X:number, Z:number)
.input b

.decl c(Y:number, Z:number)
.input c

.decl a(X:number, Y:number)

.decl res(X:number)
//.output res



//a(X, Y) :- b(X, Z), c(Y, Z).

//res(Y) :- a(1, Y).

a(X,Y) :- b(X,Z), c(Y,Z).
//res(Y) :- a(1, Y).

res(X) :- a(X, Y).
query(res(1)).