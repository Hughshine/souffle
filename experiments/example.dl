.decl C(x:number)
.input C

.decl B(x:number, y:number)
.input B

// .decl D(x:number)
// D(1).

.decl A(x:number)
.output A


A(y) :- B(x, y), C(x).

// A(x) :- C(x).

// A(y) :- B(x, y). // negation not supported yet

/* A(y) :- B(x, y), !C(x). */

/* A(y) :- A(x), B(x, y). */
